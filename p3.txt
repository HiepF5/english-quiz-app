  return (
    <div className="max-w-6xl mx-auto space-y-8 pb-16">
      
      <div className="bg-gradient-to-r from-purple-900/60 via-indigo-900/40 to-slate-900 border border-purple-500/20 rounded-3xl p-6 md:p-8 shadow-2xl relative overflow-hidden backdrop-blur-xl">
        <div className="absolute top-0 right-0 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl pointer-events-none" />
        <div className="relative z-10 flex flex-col md:flex-row items-start md:items-center justify-between gap-6">
          <div className="space-y-2">
            <div className="inline-flex items-center space-x-2 px-3 py-1 rounded-full bg-purple-500/20 text-purple-300 border border-purple-500/30 text-xs font-semibold">
              <Sparkles className="w-3.5 h-3.5 text-purple-400" />
              <span>Workspace Luy?n D?ch IELTS Speaking & Writing</span>
            </div>
            <h1 className="text-2xl md:text-3xl font-black text-white tracking-tight">
              Luy?n D?ch Theo Th? Th? & AI Ch?m ?i?m
            </h1>
            <p className="text-slate-300 text-xs md:text-sm max-w-2xl leading-relaxed">
              D?n ?o?n v?n b?n g?m c?c ch? ??, h? th?ng s? t? ??ng ph?n t?ch th?nh c?c th? ?? b?n t? g? ?o?n d?ch Ti?ng Anh. Sau ?? b?m N?p B?i ?? Gemini AI ch?m ?i?m & s?a l?i.
            </p>
          </div>

          {exercise && (
            <button
              onClick={() => {
                if (confirm('B?n c? ch?c mu?n nh?p ?? b?i m?i? B?i d?ch hi?n t?i s? ???c thay th?.')) {
                  setExercise(null);
                  setReport(null);
                  setUserAnswers({});
                }
              }}
              className="flex items-center space-x-2 px-4 py-2.5 rounded-xl bg-slate-800/80 hover:bg-slate-700 text-slate-200 border border-slate-700 text-xs font-bold transition shadow-lg shrink-0"
            >
              <RefreshCw className="w-4 h-4 text-indigo-400" />
              <span>Nh?p ?? B?i M?i</span>
            </button>
          )}
        </div>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div className="bg-slate-900/80 rounded-2xl border border-slate-800 p-4 space-y-2">
          <label className="text-xs font-bold text-slate-300 flex items-center justify-between">
            <span className="flex items-center space-x-1.5">
              <Key className="w-4 h-4 text-purple-400" />
              <span>Google Gemini API Key</span>
            </span>
            <span className="text-[11px] text-amber-400 font-semibold">(Ch? c?n khi N?p b?i AI ch?m)</span>
          </label>
          <input
            type="password"
            value={apiKey}
            onChange={(e) => setApiKey(e.target.value)}
            placeholder="AIzaSy..."
            className="w-full px-3.5 py-2.5 rounded-xl bg-slate-950 border border-slate-700 text-xs font-mono text-slate-200 focus:border-purple-500 focus:outline-none"
          />
        </div>

        <div className="bg-slate-900/80 rounded-2xl border border-slate-800 p-4 space-y-2">
          <label className="text-xs font-bold text-slate-300 flex items-center justify-between">
            <span className="flex items-center space-x-1.5">
              <GitCommit className="w-4 h-4 text-indigo-400" />
              <span>GitHub PAT Token (Push Git 1-Click)</span>
            </span>
            <span className="text-[11px] text-slate-500 font-normal">github_pat_...</span>
          </label>
          <input
            type="password"
            value={githubToken}
            onChange={(e) => setGithubToken(e.target.value)}
            placeholder="ghp_... ho?c github_pat_..."
            className="w-full px-3.5 py-2.5 rounded-xl bg-slate-950 border border-slate-700 text-xs font-mono text-slate-200 focus:border-indigo-500 focus:outline-none"
          />
        </div>
      </div>

      {errorMsg && (
        <div className="p-4 rounded-2xl bg-rose-950/40 border border-rose-500/30 text-rose-300 text-xs flex items-start space-x-3">
          <AlertCircle className="w-5 h-5 text-rose-400 shrink-0 mt-0.5" />
          <div className="leading-relaxed font-medium">{errorMsg}</div>
        </div>
      )}

      {pushResult && (
        <div className={`p-4 rounded-2xl border text-xs flex items-start space-x-3 ${
          pushResult.success 
            ? 'bg-emerald-950/40 border-emerald-500/30 text-emerald-300' 
            : 'bg-rose-950/40 border-rose-500/30 text-rose-300'
        }`}>
          <CheckCircle2 className="w-5 h-5 shrink-0 mt-0.5" />
          <div className="space-y-1">
            <p className="font-semibold">{pushResult.message}</p>
            {pushResult.url && (
              <a 
                href={pushResult.url} 
                target="_blank" 
                rel="noreferrer"
                className="inline-flex items-center space-x-1 text-emerald-400 underline font-mono text-[11px]"
              >
                <span>Xem commit tr?n GitHub</span>
                <ExternalLink className="w-3 h-3" />
              </a>
            )}
          </div>
        </div>
      )}

      {!exercise ? (
        <div className="bg-slate-900/90 rounded-3xl border border-slate-800 p-6 md:p-8 space-y-6 shadow-2xl">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-3">
              <div className="p-2.5 rounded-xl bg-purple-500/10 text-purple-400 border border-purple-500/20">
                <FileText className="w-5 h-5" />
              </div>
              <div>
                <h3 className="text-base font-bold text-white">B??c 1: Nh?p ?o?n V?n B?n B?i T?p Th?</h3>
                <p className="text-xs text-slate-400">D?n 5 ch? ?? (HOME, BIRTHDAYS, ADVERTISEMENTS, BUSES, TV SHOWS...) ?? t?o th? b?i t?p d?ch</p>
              </div>
            </div>
            <button
              onClick={() => setRawText(DEFAULT_SAMPLE_TEXT)}
              className="px-3.5 py-1.5 rounded-lg bg-slate-800 hover:bg-slate-700 text-slate-300 text-xs font-semibold border border-slate-700 transition"
            >
              T?i M?u 5 Ch? ??
            </button>
          </div>

          <div className="space-y-2">
            <textarea
              rows={14}
              value={rawText}
              onChange={(e) => setRawText(e.target.value)}
              placeholder="D?n n?i dung c?u h?i & b?i tr? l?i theo t?ng ch? ?? t?i ??y..."
              className="w-full p-4 rounded-2xl bg-slate-950 border border-slate-700 text-xs font-mono text-slate-200 focus:border-purple-500 focus:outline-none leading-relaxed resize-y"
            />
            <p className="text-[11px] text-slate-400 italic">
              ?? M?o: H? th?ng t? ??ng nh?n di?n c?c ti?u ?? d?ng <code className="text-purple-300">1. HOME</code>, <code className="text-purple-300">2. BIRTHDAYS</code>... ?? ph?n nh?m b?i t?p.
            </p>
          </div>

          <div className="pt-2 flex flex-col sm:flex-row items-center justify-between gap-4 border-t border-slate-800">
            <div className="text-xs text-slate-400">
              <span className="font-semibold text-emerald-400">? T? ??ng & Kh?ng c?n API Key</span> ?? t?o th? d?ch!
            </div>
            <button
              disabled={isGenerating}
              onClick={handleStartPractice}
              className="w-full sm:w-auto px-8 py-3.5 rounded-xl bg-gradient-to-r from-purple-600 via-indigo-600 to-blue-600 hover:from-purple-500 hover:to-blue-500 text-white font-bold text-xs shadow-xl shadow-purple-600/20 transition flex items-center justify-center space-x-2"
            >
              {isGenerating ? <Loader2 className="w-4 h-4 animate-spin" /> : <Sparkles className="w-4 h-4" />}
              <span>{isGenerating ? '?ang T?o Th? B?i T?p...' : '?? B?t ??u Luy?n D?ch (T?o Th? D?ch)'}</span>
            </button>
          </div>
        </div>
      ) : (

        <div className="space-y-6">

          <div className="bg-slate-900/90 rounded-2xl border border-slate-800 p-5 space-y-4 shadow-xl">
            <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
              <div>
                <h3 className="text-sm font-bold text-white flex items-center space-x-2">
                  <span>{exercise.title}</span>
                  <span className="text-xs px-2.5 py-0.5 rounded-full bg-purple-500/20 text-purple-300 border border-purple-500/30">
                    {exercise.items.length} c?u h?i
                  </span>
                </h3>
                <p className="text-xs text-slate-400 mt-0.5">{exercise.description}</p>
              </div>

              <div className="flex items-center space-x-3 w-full sm:w-auto justify-between sm:justify-end">
                <div className="text-right">
                  <div className="text-xs font-bold text-indigo-300">
                    ?? ho?n th?nh: {answeredCount} / {exercise.items.length} c?u
                  </div>
                  <div className="text-[11px] text-slate-400">
                    ({Math.round((answeredCount / exercise.items.length) * 100)}%)
                  </div>
                </div>

                <div className="w-24 h-2 rounded-full bg-slate-800 overflow-hidden">
                  <div 
                    className="h-full bg-gradient-to-r from-purple-500 to-emerald-400 transition-all duration-300"
                    style={{ width: `${(answeredCount / exercise.items.length) * 100}%` }}
                  />
                </div>
              </div>
            </div>

            {topics.length > 0 && (
              <div className="flex items-center space-x-2 overflow-x-auto pb-1 border-t border-slate-800/80 pt-3 text-xs scrollbar-none">
                <span className="text-slate-400 font-semibold flex items-center space-x-1 shrink-0">
                  <Layers className="w-3.5 h-3.5 text-purple-400" />
                  <span>Topic:</span>
                </span>
                <button
                  onClick={() => setSelectedTopic('ALL')}
                  className={`px-3 py-1.5 rounded-xl font-bold transition shrink-0 ${
                    selectedTopic === 'ALL'
                      ? 'bg-purple-600 text-white shadow-md'
                      : 'bg-slate-800/80 hover:bg-slate-700 text-slate-300'
                  }`}
                >
                  T?t C? ({exercise.items.length})
                </button>

                {topics.map((top) => {
                  const count = exercise.items.filter(i => i.topic === top).length;
                  return (
                    <button
                      key={top}
                      onClick={() => setSelectedTopic(top)}
                      className={`px-3 py-1.5 rounded-xl font-bold transition shrink-0 ${
                        selectedTopic === top
                          ? 'bg-indigo-600 text-white shadow-md'
                          : 'bg-slate-800/80 hover:bg-slate-700 text-slate-300'
                      }`}
                    >
                      {top} ({count})
                    </button>
                  );
                })}
              </div>
            )}
          </div>

          {report && (
            <div className="bg-gradient-to-r from-purple-950/60 via-slate-900 to-indigo-950/60 border border-purple-500/30 rounded-2xl p-6 space-y-4 shadow-xl">
              <div className="flex flex-col sm:flex-row items-center justify-between gap-4 border-b border-purple-500/20 pb-4">
                <div className="flex items-center space-x-3">
                  <div className="p-3 rounded-2xl bg-purple-500/20 text-purple-300 border border-purple-500/30">
                    <Award className="w-7 h-7 text-purple-400" />
                  </div>
                  <div>
                    <h3 className="text-base font-bold text-white">K?t Qu? Ch?m ?i?m T? Gemini AI</h3>
                    <p className="text-xs text-slate-400">?? ??nh gi? to?n b? {report.feedbackItems.length} c?u b?i l?m</p>
                  </div>
                </div>

                <div className="flex items-center space-x-4">
                  <div className="p-3 rounded-2xl bg-slate-900/80 border border-slate-700 text-center min-w-[100px]">
                    <span className="text-[11px] text-slate-400 font-semibold uppercase">Thang ?i?m 10</span>
                    <div className="text-2xl font-black text-purple-400 mt-0.5">{report.overallScore} / 10</div>
                  </div>

                  <div className="p-3 rounded-2xl bg-slate-900/80 border border-slate-700 text-center min-w-[100px]">
                    <span className="text-[11px] text-slate-400 font-semibold uppercase">T? L? Chu?n</span>
                    <div className="text-2xl font-black text-emerald-400 mt-0.5">{report.overallPercentage}%</div>
                  </div>
                </div>
              </div>

              <div className="p-4 rounded-xl bg-slate-950/60 border border-slate-800 text-xs text-slate-200 leading-relaxed">
                <strong className="text-purple-300 font-bold">Nh?n x?t gi?m kh?o AI:</strong> {report.evaluationComment}
              </div>
            </div>
          )}

          <div className="space-y-6">
            {filteredItems.map((item, idx) => {
              const itemFB = report?.feedbackItems.find(fb => fb.itemId === item.id);
              const charCount = (userAnswers[item.id] || '').length;
              const wordCount = (userAnswers[item.id] || '').trim().split(/\s+/).filter(Boolean).length;

              return (
                <div key={item.id} className="bg-slate-900/90 rounded-2xl border border-slate-800 p-6 space-y-4 shadow-xl">
                  
                  <div className="flex items-center justify-between">
                    <div className="flex items-center space-x-2">
                      <span className="px-3 py-1 rounded-lg bg-indigo-500/10 text-indigo-300 border border-indigo-500/20 text-xs font-bold">
                        C?u {idx + 1}
                      </span>
                      <span className="text-xs font-bold text-purple-400 uppercase tracking-wider">
                        Topic: {item.topic}
                      </span>
                    </div>

                    {itemFB && (
                      <span className="text-xs font-bold px-3 py-1.5 rounded-xl bg-purple-500/20 text-purple-300 border border-purple-500/30">
                        ?i?m: {itemFB.score} / 10
                      </span>
                    )}
                  </div>

                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 bg-slate-950/60 p-4 rounded-xl border border-slate-800/80 text-xs space-y-2 md:space-y-0">
                    <div>
                      <span className="font-bold text-indigo-400 uppercase tracking-wider block mb-1">?o?n V?n M?u Ti?ng Anh:</span>
                      <p className="font-semibold text-slate-200 leading-relaxed mb-1">{item.originalEnglishQuestion}</p>
                      <p className="text-slate-300 leading-relaxed italic">{item.originalEnglishAnswer}</p>
                    </div>

                    <div className="border-t md:border-t-0 md:border-l border-slate-800 pt-3 md:pt-0 md:pl-4">
                      <span className="font-bold text-purple-400 uppercase tracking-wider block mb-1">G?i ? N?i Dung Ti?ng Vi?t:</span>
                      <p className="font-semibold text-slate-200 leading-relaxed mb-1">{item.vietnamesePromptQuestion}</p>
                      <p className="text-slate-300 leading-relaxed">{item.vietnamesePromptAnswer}</p>
                    </div>
                  </div>

                  <div className="space-y-2">
                    <div className="flex items-center justify-between">
                      <label className="text-xs font-bold text-slate-200 flex items-center space-x-1.5">
                        <span className="w-2 h-2 rounded-full bg-emerald-400 animate-pulse" />
                        <span>T? nh?p ?o?n d?ch Ti?ng Anh c?a b?n:</span>
                      </label>

                      <div className="text-[11px] text-slate-500 space-x-2">
                        <span>{wordCount} t?</span>
                        <span>?</span>
                        <span>{charCount} k? t?</span>
                      </div>
                    </div>

                    <textarea
                      rows={3}
                      value={userAnswers[item.id] || ''}
                      onChange={(e) => handleAnswerChange(item.id, e.target.value)}
                      placeholder="G? ?o?n d?ch Ti?ng Anh c?a b?n t?i ??y..."
                      className="w-full p-4 rounded-xl bg-slate-950 border border-slate-700 text-xs font-medium text-slate-100 focus:border-indigo-500 focus:outline-none leading-relaxed resize-y"
                    />
                  </div>

                  {itemFB && (
                    <div className="p-4 rounded-xl bg-slate-950/80 border border-purple-500/30 text-xs space-y-3 animate-fadeIn">
                      <div className="flex items-center space-x-2 font-bold text-purple-300 border-b border-slate-800 pb-2">
                        <FileCheck className="w-4 h-4 text-purple-400" />
                        <span>AI ??nh Gi? & Ch?nh S?a Chi Ti?t:</span>
                      </div>

                      <div className="p-3 rounded-lg bg-emerald-950/30 border border-emerald-500/30 text-emerald-200 space-y-1">
                        <strong className="text-emerald-400 block font-bold">C?u s?a ho?n ch?nh t? nhi?n nh?t:</strong>
                        <p className="leading-relaxed font-semibold">{itemFB.correctedEnglish}</p>
                      </div>

                      {itemFB.grammarErrors.length > 0 && (
                        <div className="text-rose-300 space-y-1">
                          <strong className="text-rose-400 block font-bold">L?i ng? ph?p c?n l?u ?:</strong>
                          <ul className="list-disc list-inside space-y-0.5 text-slate-300">
                            {itemFB.grammarErrors.map((err, eIdx) => (
                              <li key={eIdx}>{err}</li>
                            ))}
                          </ul>
                        </div>
                      )}

                      {itemFB.vocabularySuggestions.length > 0 && (
                        <div className="text-indigo-300 space-y-1">
                          <strong className="text-indigo-400 block font-bold">G?i ? t? v?ng & C?m t? n?ng band:</strong>
                          <ul className="list-disc list-inside space-y-0.5 text-slate-300">
                            {itemFB.vocabularySuggestions.map((sug, sIdx) => (
                              <li key={sIdx}>{sug}</li>
                            ))}
                          </ul>
                        </div>
                      )}

                      <div className="text-slate-300 border-t border-slate-800 pt-2 leading-relaxed">
                        <strong className="text-slate-400 block font-semibold mb-0.5">Gi?i th?ch chi ti?t:</strong>
                        <p>{itemFB.explanation}</p>
                      </div>
                    </div>
                  )}

                </div>
              );
            })}
          </div>

          <div className="p-6 rounded-2xl bg-slate-900 border border-slate-800 flex flex-col sm:flex-row items-center justify-between gap-4 shadow-xl">
            <div>
              <h4 className="text-sm font-bold text-white">Ho?n Th?nh B?i Luy?n D?ch?</h4>
              <p className="text-xs text-slate-400">B?m n?p b?i ?? Gemini AI nh?n x?t, ch?m ?i?m v? s?a l?i to?n b? b?i d?ch c?a b?n.</p>
            </div>

            <div className="flex flex-wrap items-center gap-3 w-full sm:w-auto">
              <button
                onClick={handleExportUserWork}
                className="flex-1 sm:flex-none flex items-center justify-center space-x-2 px-4 py-3 rounded-xl bg-slate-800 hover:bg-slate-700 text-slate-200 text-xs font-bold border border-slate-700 transition"
              >
                <Download className="w-4 h-4 text-emerald-400" />
                <span>Export (.md)</span>
              </button>

              <button
                disabled={isPushing}
                onClick={handlePushGit}
                className="flex-1 sm:flex-none flex items-center justify-center space-x-2 px-4 py-3 rounded-xl bg-slate-800 hover:bg-slate-700 text-slate-200 text-xs font-bold border border-slate-700 transition"
              >
                {isPushing ? <Loader2 className="w-4 h-4 animate-spin text-indigo-400" /> : <GitCommit className="w-4 h-4 text-indigo-400" />}
                <span>Push Git</span>
              </button>

              <button
                disabled={isGrading}
                onClick={handleSubmitGrading}
                className="flex-1 sm:flex-none flex items-center justify-center space-x-2 px-6 py-3 rounded-xl bg-gradient-to-r from-purple-600 via-indigo-600 to-blue-600 hover:from-purple-500 hover:to-blue-500 text-white font-bold text-xs shadow-lg shadow-purple-600/25 transition"
              >
                {isGrading ? <Loader2 className="w-4 h-4 animate-spin" /> : <Send className="w-4 h-4" />}
                <span>{isGrading ? 'AI ?ang Ch?m ?i?m...' : 'N?p B?i & AI Ch?m ?i?m'}</span>
              </button>
            </div>
          </div>

        </div>
      )}

    </div>
  );
};
